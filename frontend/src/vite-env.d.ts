/// <reference types="svelte" />
/// <reference types="vite/client" />

// Replaced at build time by vite.config.ts from the APP_VERSION environment
// variable, which the release workflow sets from the git tag.
declare const __APP_VERSION__: string
